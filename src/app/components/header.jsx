'use strict';

import React from 'react';
import {Link} from 'react-router';

const Header = () => {
    return (
        <header className="container">
            Header
        </header>
    )
};

export default Header;
